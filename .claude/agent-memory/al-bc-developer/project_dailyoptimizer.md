---
name: project-dailyoptimizer
description: Key facts about the DailyOptimizer extension — object ranges, architecture, active development areas
metadata:
  type: project
---

**Extension:** DailyOptimizer (app version 28.0.0.0), publisher Optimizers, app.json `application` field still declares 26.0.0.0 / `runtime` 15.0 (legacy, left over from the "convert to v.28" commit 703df22 which only bumped the app's own `version` + added `preprocessorSymbols: ["BC28"]` — did not bump `application`/`runtime`). NoImplicitWith enabled.

**ID Range:** 50600–60700 (custom objects). DateEngine demo objects occupy 60001–60016 (hand-rolled, non-BC-Test-framework harness under `src/dhx/ganttdemo2/DateEngine/` — ignore as a pattern). Test codeunits added 2026-06-16: 60020 "Day Planning Creation Tests", 60021 "Day Planning Test Runner" in `test/`. Query 50607 "Resource List Sections" added 2026-07-10 (`src/query/query_50607_ResourceListSections.al`) — next free query ID is 50608. Existing queries 50600-50606 pre-date it; 50607 was previously mistakenly assumed free by ID-gap alone but WAS actually free — confirm with a fresh `Glob src/query/*.al` before reusing, don't trust this note's numbers forever.

**Core domain:** Project/resource scheduling on top of standard BC Jobs/Job Tasks. Key custom tables (current names, post-rename — memory previously had stale "Day Tasks"/"Day Task Generator" names, corrected 2026-06-16):
- `Day Planning` (table 50610) — daily task assignments per Job/Job Task, PK is (Job No., Job Task No., Day Line No.)
- `Day Planning Pattern` (table 50607) — pattern record (date range + Day 1..7 weekday checkboxes + work-hour template) expanded into Day Planning lines
- `Daily Optimizer Setup` (table 50605) — singleton, `"Base Calendar"` field (TableRelation to standard `Base Calendar`) is required by `ExpectedWeekDay` in codeunit 50610

**Key codeunits:**
- 50610 `Day Plannings Mgt.` — `CreateDayPlanning(DayPlanningPattern: Record "Day Planning Pattern")` expands a pattern into Day Planning records. TestField on "Work-Hour Template" runs BEFORE the `case true of` validation block, so a blank Work-Hour Template raises the platform TestField error, not the custom "Work-Hour Template must be specified" text in that same case block (dead branch for that specific scenario).
- 50612 `General Planning Utilities` — `DateToInteger`, capacity/fulfillment calc helpers
- 50613/50614/50615 — Gantt chart data/page/update handlers (DHTMLX integration)
- 50617 `DayPlanning Period Sync Mgt.` (file still named `codeunit_50617_DayPlanningPeriodSyncMgt.al` under `src/dhx/ganttdemo2/`) — preview + apply Day Planning date shifts when Job Task period changes

**Why:** Planning and scheduling ISV solution using DHTMLX Gantt/Scheduler controls embedded in BC pages.
**How to apply:** When adding features, check these codeunits first before touching page/table triggers. See [[dailyoptimizer-test-framework-setup]] before adding any new `[Test]` codeunits.

**Mandatory Schedulling resources always visible in Pool Resource panel (added 2026-07-10):** Resource field `"Mandatory Schedulling"` (field 50620 on tableextension 50603 "Resource Opt", note the double-L spelling — existing name, don't "fix" it) now forces a resource to always appear in page 50600's y-axis tree, even with zero `Res. Capacity Entry` rows in the visible date range. Implemented via new query 50607 "Resource List Sections" (top dataitem Resource, LeftOuterJoin child "Res. Capacity Entry" with a `CapacityEntryCount` Count column) replacing query 50601 "Unique Resource in Capacity" inside `GetUniqueResFromCapacity_Pool`'s `(TempRes; TempPoolRes; ResGroupNo; StartDate; EndDate)` overload in `codeunit_50604_DHXDataHandler.al` — include-row rule is `Mandatory_Schedulling or (CapacityEntryCount > 0)`, and it also drops the old per-row `Res.Get(ResNo)` N+1 call (the per-unique-Pool-header `Res.Get(PoolNo)` lookup for `TempPoolRes` stays, that one was already O(unique pools) not O(resources)). `GetUniqueResGroupFromCapacity` also got a small unconditional `Resource.SetRange("Mandatory Schedulling", true)` loop appended (no new query — deliberately NOT using the join+count pattern here since group visibility for mandatory resources doesn't depend on any date range) so a Resource Group made up entirely of otherwise-empty mandatory resources still gets its group node in the tree; the tree-render loop in `GetYUnitElementsJSON_Pool` already skips empty Pool/Group nodes dynamically by array-count, so no change was needed there. The other overload `GetUniqueResFromCapacity_Pool(TempRes; ResGroupNo; PoolNo; StartDate; EndDate)` and `GetYUnitElementsJSON_Project`/`_Resource` were explicitly out of scope and left untouched.
**How to apply:** If the Resource-variant scheduler ever needs the same mandatory-visibility rule, mirror query 50607 + the two include-rule checks rather than sharing code — this codebase's established convention (see pagination note below) is to keep Pool vs Resource variants separate rather than share.

**Pool Resource Scheduler pagination (added 2026-07-08):** Page 50600 "DHX Scheduler (Pool Resource)" only ever renders one resource page at a time (default 200) instead of the whole company (~34,725 resources) because DHTMLX Scheduler has no server-side pagination hook. Pattern: `codeunit_50604_DHXDataHandler.al` has `GetYUnitElementsJSON_Pool` (now takes `var PageSkip`, `PageSize`, `var TotalResourceCount`) and a new `local procedure CollectPageResourceNos_Pool` that walks the SAME ResGroup→Pool→Resource nested loop as the tree-builder purely to count leaves and build a page-window `Dictionary of [Code[20], Boolean]` — this doubles the enumeration cost of that nested loop (acceptable; it's not proportional to full JSON-building cost) but only when the requested page turns out to be out of range does it re-run a second time to rebuild the dictionary against the clamped skip. A separate public `FindResourcePageIndex` reruns the same walk to resolve a searched resource's 0-based position for the page's search box. The events query (query 50604 `Capacity Per Day Per Resource`) is bounded to the page via its previously-unused `Resource_No__Filter` filter element, OR-joined (`'RES1|RES2|...'`) — confirmed safe because `Query.SetFilter`'s filter-expression parameter is always unbounded `Text` regardless of the underlying field's type (`Code[20]`), as long as the filter-building local variable is also declared as plain `Text` (not `Text[N]`).
**How to apply:** If the Resource-variant scheduler (`GetYUnitElementsJSON_Resource`) ever needs the same treatment, mirror this pattern rather than sharing code with the Pool variant — the brief explicitly kept them separate (`GetUniqueResGroupFromCapacity`'s shared signature was NOT touched).

**Embedded resource files (added 2026-07-08):** Bundling a static file (e.g. CSV lookup data) into the `.app` requires declaring `"resourceFolders": ["src/data"]` in `app.json` — this is a BC 2024 wave 2 / update 25.2+ feature and is NOT automatic; plain loose files anywhere in the project (project root, an undeclared folder, etc.) are silently dropped from the package even though `al_build` reports success. Verified by actually unzipping the built `.app` (it's a ZIP with a ~40-byte header before the `PK\x03\x04` signature) and checking for a `PackagedResources.json` entry plus `resources/<file>` entries. `NavApp.GetResourceAsText(ResourceName, TextEncoding::UTF8)` (and `GetResourceAsStream`/`GetResourceAsJson`) take `ResourceName` **relative to the declared folder**, not the project-relative path — e.g. `src/data/FirstNames.csv` on disk is read back as just `'FirstNames.csv'`. `codeunit_50602_CreateDemoData.al`'s `GetNextName()` uses these CSVs (635 first names x 1000 last names = 635,000 combos) with a collision-free permutation — `PermutedIndex := (Counter * 392453) mod 635000` computed in `BigInteger` (the raw `Counter * Multiplier` product overflows 32-bit `Integer` well before the ~34,725 resources created per demo-data run) — 392453 was chosen because it's coprime with 635000 (`= 2^3 * 5^4 * 127`), making the mapping a bijection over `[0, 635000)` so every drawn name pair is unique as long as draws stay under 635,000.
**Why:** User showed a screenshot of many demo Resources all named "Jennifer Martinez" — the old `GetRandomName()` drew from only 20x20=400 combos via `Random()`, guaranteeing duplicates at ~34,725 draws.

**`codeunit_50602_CreateDemoData.al` demo-data generation architecture (updated 2026-07-09):** `CreateDemoResources()` now calls `CreateDemoVendors()` (before `LoadReferenceData()`) which inserts/upserts 50 fixed real construction-company vendors `DRV001..DRV050` into a dedicated `gDemoVendorNos` list — `CreatePoolResource()` draws its Vendor (and uses that Vendor's Name as the Resource Name) via `GetNextDemoVendor()`/`gDemoVendorNos` specifically, NOT the general `GetNextVendor()`/`gVendorNos` pool (which mixes in whatever pre-existing vendors the company already has). `CreateExternalResource()` still uses the general `GetNextVendor()`. Bulk jobs (`DJB0001..DJB0170`, via `CreateBulkJobs()`/`CreateBulkJob()`/`CreateBulkJobTasks()`) now get intuitive construction-industry names instead of "Demo Bulk Project 0001"/"Phase 1"/"Phase 1 Task 1": job names are a bijective site+type combo via `GetUniqueDemoJobName(SequenceNo)` (same permutation technique as `GetUniqueDemoResourceName`, multiplier `104729`, fed 4 new CSVs in `src/data/`: `JobSiteNames.csv`, `ProjectTypeNames.csv`, `JobPhaseNames.csv`, `JobTaskNames.csv`), while phase/task names are a FIXED set (4 phases x 5 tasks) reused identically across all 170 bulk jobs via `GetJobPhaseName`/`GetJobTaskName`. JOB001/002/003 (the 3 showcase jobs) are untouched by any of this.

**Bug fix (2026-07-09): blank "Daily Optimizer Setup" singleton, superseded same day by "always force" (see below).** `page_50654_DailyOptimizerSetup.al`'s `OnOpenPage` auto-inserts a BLANK singleton record the first time anyone opens that page (`if not Rec.Get() then Rec.Insert()`), independent of whether Create Demo Data has run. `CreateDailyOptimizerSetupDefault()` used to whole-record-exit via `if Setup.Get() then exit;`, so if the page had already been opened once, the blank record "existed" and the procedure silently skipped populating every field — including `Base Calendar`, which `codeunit_50610`'s `TestField("Base Calendar")` depends on downstream. First fix attempt switched to per-field blank checks (`if Setup."Base Calendar" = '' then ...`); also: the demo Base Calendar code changed from reusing generic `'BASIS'` to a dedicated `Code = 'DEMOCAL'` / `Name = 'DEMOPLANNING'` (Code is `Code[10]`, too short for "DEMOPLANNING" (12 chars), so the full label lives in Name instead) via new `CreateDemoCalendar()` (replaces the now-deleted `EnsureBaseCalendar()`), which — unlike the other `Ensure*` helpers in this codeunit that are permanent/unlogged — IS `LogRecord`'d and recreated fresh every run, avoiding collision with whatever calendar setup already exists in the target sandbox.

**Current behavior (as of 2026-07-09, current):** the per-field blank-check turned out insufficient — an environment whose Daily Optimizer Setup already had `Base Calendar = 'BASIS'` from an older run (pre-dating the `DEMOCAL` change) kept that value forever, since blank-check only fills empties. User confirmed twice that "Create Demo Data" should fully own this singleton every run, so `CreateDailyOptimizerSetupDefault()` now UNCONDITIONALLY assigns all 5 fields (`Base Calendar := CalendarCode` ('DEMOCAL'), `Work hour Template := 'BASIS'`, `Default Skill := 'ELEKTR'`, `Order Intake Nos := 'OI'`, `Work Order Nos := 'WO'`) every run — insert-if-missing via `if not Setup.Get() then ...`, then always overwrite, then `Setup.Modify()`. This is a deliberate exception to this codeunit's general "Ensure*-style permanent/unlogged, never touch again" pattern used elsewhere (e.g. `CreateGanttChartSetupDefaults`) — the Daily Optimizer Setup singleton is demo-data-owned, not user-preserved.
**How to apply:** Don't reintroduce blank-checks here without re-confirming with the user — it was already tried and explicitly rejected in favor of unconditional overwrite. For other setup/singleton seeding elsewhere in the app, default to the blank-check pattern (least surprising, preserves user customization) unless the singleton is specifically demo-data-owned like this one.

**`CreateDemoCalendar()` calendar exceptions (added 2026-07-09, revised same day to add Annual Recurring + Year-2 custom rows):** `CreateDemoCalendar()` calls `CreateDemoCalendarChanges(CalendarCode)` (right before `exit`), which populates `"Base Calendar Change"` (NOT `"Customized Calendar Change"` — that's a different, derived/combined table used at read-time, e.g. by `ExpectedWeekDay` in codeunit 50610) via `LoadDutchPublicHolidays` + 8 direct `InsertBaseCalendarChange` calls for custom demo days-off. `InsertBaseCalendarChange(CalendarCode, ChangeDate, Desc, RecurringSystem: Integer)` is the idempotent Get-then-Insert primitive (PK is `"Base Calendar Code", "Recurring System", Date, Day` — `Day` left at default 0/blank, only relevant for Weekly Recurring which this codeunit never uses), `LogRecord`'d so `DeleteDemoData()` wipes and recreates these every run.
- **`src/data/DutchPublicHolidays.csv`** now has a 3rd column `Recurring` (`Y`/`N`). Of the 10 Dutch holidays, only 4 are on a truly fixed calendar date every year (Nieuwjaarsdag Jan 1, Bevrijdingsdag May 5, Eerste/Tweede Kerstdag Dec 25/26) — these appear as a SINGLE `Y` row each (one anchor date, not one row per year 2025-2030) and are inserted UNCONDITIONALLY (no `[gStartDate,gEndDate]` window filter) with `"Recurring System" = Annual Recurring`. The other 6 (Eerste/Tweede Paasdag, Hemelvaartsdag, Eerste/Tweede Pinksterdag — all move with the lunar Paschal/Easter cycle — and Koningsdag, which is normally fixed Apr 27 but shifts to Apr 26 in the rare year it lands on a Sunday e.g. 2025) stay `N` rows, one per year 2025-2030, window-filtered as before — converting these to Annual Recurring would repeat the WRONG date most years, a real bug not a simplification.
- **`"Base Calendar Change"."Recurring System"` (Option) values — do NOT guess, confirmed by decompiling the actual base-app table source** (table 7601, `src/Foundation/Calendar/BaseCalendarChange.Table.al` inside the extracted `Microsoft_Base Application_*.app`, since `al_symbolsearch` only returns `"Recurring System": Option` with no member list for base-app Option fields — see the general decompile technique note below): `OptionMembers = " ","Annual Recurring","Weekly Recurring"` → **0 = blank/non-recurring, 1 = Annual Recurring, 2 = Weekly Recurring**. `InsertBaseCalendarChange`'s `RecurringSystem` parameter is typed `Integer` (not the table's own Option type — AL doesn't let an anonymous/foreign Option type cross a procedure boundary the way Enums do) and assigned directly into the Option field, which AL permits without an explicit cast; three tiny named helpers `CalRecNonRecurring(): Integer` (0), `CalRecAnnualRecurring(): Integer` (1), `CalRecWeeklyRecurring(): Integer` (2, added 2026-07-09 for the weekend-marker rows below) make call sites read as intent instead of bare magic-number literals. Also confirmed: `"Base Calendar Change"`'s `OnInsert`/`OnModify`/`OnRename` triggers run `CheckEntryLine()`, which `TestField`s `Day` even for blank/non-recurring rows in a way that would reject our Day-left-blank inserts — but this is a non-issue because `InsertBaseCalendarChange`/`InsertWeeklyRecurringDayOff` both call bare `BaseCalendarChange.Insert()` (no `true` argument), and AL's parameterless `Insert()` defaults to `RunTrigger = false`, so `OnInsert`/`CheckEntryLine()` never fires.
- **`"Base Calendar Change".Day` (Option) values — also confirmed by decompiling the same table source**, same file/field(4): `OptionMembers = " ",Monday,Tuesday,Wednesday,Thursday,Friday,Saturday,Sunday` → **0 = blank, 1 = Monday .. 7 = Sunday**. This happens to numerically match `Date2DWY(D,1)`'s 1=Monday..7=Sunday convention used elsewhere in this codeunit, but that's a confirmed coincidence (verified independently, not assumed) — this Option field is unrelated to `Date2DWY`. Named helpers `CalDaySaturday(): Integer` (6) / `CalDaySunday(): Integer` (7).
- **Weekly-recurring weekend markers (added 2026-07-09):** `CreateDemoCalendarChanges()` also calls new `InsertWeeklyRecurringDayOff(CalendarCode, DayOfWeek)` twice, for `CalDaySaturday()`/`CalDaySunday()` — inserts a `"Recurring System" = Weekly Recurring` row with `Date = 0D`, `Day = <weekday>`, `Nonworking = true`, blank `Description` (2 rows, PK is Date=0D + real Day, the OPPOSITE shape from every other row in this codeunit which uses a real Date + Day=0 — hence a separate insert helper rather than an `InsertBaseCalendarChange` overload). Purpose: without these, DEMOCAL's own Base Calendar Change data has no exception marking Sat/Sun non-working — this codeunit's `IsWorkingDay()` doesn't care (it checks the Work-Hour Template BEFORE ever consulting DEMOCAL), but any OTHER code that queries DEMOCAL's calendar directly via `Calendar Management.IsNonworkingDay()`/`SetSource()` without a separate Work-Hour Template check would otherwise incorrectly treat Saturdays/Sundays as working days. Makes DEMOCAL correct/self-sufficient as a standalone calendar object.
- **Custom demo days-off, FINAL shape (superseded 4x same day — 4 rows → 8 rows → 36 rows → 144-via-calendar-month-arithmetic → 144-via-simple-4-week-loop, current/final is the last one):** `CreateDemoCalendarChanges()` is back to the ORIGINAL 4-line week-offset formula relative to `gStartDate` (`CalcDate('+1W+1D', MonthAnchor)` = Wk2 Tue, `'+2W+2D'` = Wk3 Wed, `'+2W+3D'` = Wk3 Thu, `'+4W'` = Wk5 Mon — same formulas/description text as the very first 4-row version) — but now wrapped in `for MonthIndex := 0 to 35 do` with `MonthAnchor := CalcDate(StrSubstNo('+%1W', MonthIndex * 4), gStartDate)`, i.e. the same 4-pattern block repeated every 4 weeks for 36 iterations (~3 years) = 144 rows total. **No calendar-month/Nth-weekday-of-month arithmetic at all** — `GetNthWeekdayOfMonth`/`InsertMonthlyCustomDayOff`/`GetSecondTuesdayOfMonth` (all 3 helpers from earlier revisions) are fully removed, confirmed via repo-wide grep, no leftover references. Still non-recurring (`CalRecNonRecurring()`) — offsets from THIS RUN's rolling `gStartDate`, not a fixed calendar date/weekday. Duplicate Description text across the 36 occurrences of each pattern is fine — `"Base Calendar Change"`'s PK is Base Calendar Code + Recurring System + Date + Day, not Description. **If asked to touch this again, this simple 4-week-loop version is the user's confirmed final preference — don't reintroduce calendar-month arithmetic (Nth-weekday-of-month, `DMY2Date`/`Date2DMY` gymnastics) unless explicitly asked again.**

**Blank `Skill` on Day Planning fix — `EnsureSkillCodes()`/`EnsureLoadedResourceSkills()` (added 2026-07-09):** Root cause: `GetResourceSkill(ResNo)` returns blank when a resource has no `Resource Skill` record. The 200 demo resources (`DRP*`/`DRM*`/`DRE*`) always get one via `AssignResourceSkill()` at creation, but `GetRes(1)`-`GetRes(9)` (JOB001-003's leader/member, plus `CreateWorkOrderDemoData`'s `GetRes(7)`/`GetRes(8)`/`GetRes(9)`) draw from `gRes[]`, populated by `LoadResources()` from PRE-EXISTING real company resources — nothing assigned those a skill. Fixed with 2 new pieces: (1) `EnsureSkillCodes()` (mirrors `EnsureResourceGroups()`'s "only seed if table completely empty" pattern exactly, permanent/unlogged) seeds 10 construction-themed Skill Codes (ELEKTR/MECH/CIVIL/WELD/PLUMB/CARP/CRANE/SURVEY/SAFETY/GENERAL) instead of the single `'ELEKTR'` `CreateDailyOptimizerSetupDefault()` used to be the only seeder of. (2) `EnsureLoadedResourceSkills()`/`EnsureResourceHasSkill(ResNo)` backfill a RANDOM (`Random(gSkillCodes.Count())`, not round-robin — user explicitly asked for random, matches the existing `Res."Mandatory Schedulling" := (Random(100) <= 60)` precedent in `SetMandatoryResourceFields`) skill onto any `gRes[]` resource that doesn't already have one; logged via `LogRecord` (disposable, `DeleteDemoData()`-cleaned, re-evaluated fresh every run) — unlike `EnsureSkillCodes()` which is permanent. **Exact `OnRun()` sequencing matters and was explicitly specified by the user**: `EnsureSkillCodes()` goes right after `CreateBulkJobs()`, BEFORE both `LoadResources()`/`CreateDemoResources()` (so `CreateDemoResources()`'s internal `LoadReferenceData()` picks up the full 10-code set, and the 200 demo resources' own round-robin `GetNextSkill()` has real variety) — `EnsureLoadedResourceSkills()` goes AFTER the zero-resource-fallback `LoadResources()` re-check (so `gRes[]`/`gResCount` are in their FINAL state) but BEFORE `CreateDailyOptimizerSetupDefault()`/`CreateCapacityAndDayPlanning()`/`CreateWorkOrderDemoData()` (the latter two call `GetResourceSkill()` while generating Day Planning rows).

**Mandatory Skill on Day Planning resource fields (added 2026-07-09), `table_50610_DayPlanning.al`:** `"Assigned Resource No."` (field 21) and `"Requested Resource No."` (field 27) OnValidate now both call new `local procedure CheckResourceHasSkill(ResNo)` (Errors: `'Resource %1 does not have a Skill assigned.'` if the resource has zero `Resource Skill` rows) and auto-populate `Skill` via new `local procedure GetFirstSkill(ResNo): Code[20]` (caller must call `CheckResourceHasSkill` first — no internal guard, relies on caller order). Assigned Resource's check/assignment is unconditional; Requested Resource's check is ALSO unconditional but the `Skill :=` assignment only happens inside the pre-existing `if "Assigned Resource No." = '' then` block — this follows the table's ALREADY-ESTABLISHED "Assigned is dominant over Requested" convention (same block that already gates Vendor No./Leader/Team Leader cascade), not a new rule. `field(24; "Skill")`'s own separate/narrower OnValidate (checks a manually-typed Skill against the Assigned Resource) was left untouched. Confirmed safe against `codeunit_50602_CreateDemoData.al`'s demo generator: `InsertLeaderDayPlanning`/`InsertMemberDayPlanning`/`InsertWorkOrderDayPlanning` all set these fields via DIRECT FIELD ASSIGNMENT (never `Validate()`) and `Insert(false)`, so OnValidate never fires there regardless — and the demo generator independently guarantees skill coverage via `EnsureLoadedResourceSkills()` (see above) anyway. `CopyRequestedToAssigned()` (same table, fixed earlier this session) also confirmed unaffected — it sets `"Assigned Resource No."` via direct assignment too (`Rec."Assigned Resource No." := Rec."Requested Resource No.";`), not `Validate()`.
**How to apply / real-world impact found via grep, not anticipated by the original request:** grepping `src/` for `Validate("Assigned Resource No.", ...)`/`Validate("Requested Resource No.", ...)` beyond table_50610's own `OnLookup` triggers turned up ONE more live call site: `src/dhx/ganttdemo2/page_50620_GanttDemo.al`'s `onAddDayPlanning` trigger (JS-invoked when a user drags/drops onto a resource lane in the DHTMLX Gantt chart) — a real user-facing entry point that now inherits this mandatory check. Fixed the same day (see next entry) so it fails cleanly instead of raw/unhandled. If any FUTURE caller anywhere else in the app calls `.Validate()` (not direct assignment) on either of these two fields, it will also now inherit this mandatory-skill check — worth grepping for new call sites again if this table's resource-field validation is touched again.

**Friendly error surfacing for `page_50620_GanttDemo.al`'s `onAddDayPlanning` (added 2026-07-09):** The Gantt drag-to-add flow's `DayPlanning.Validate("Requested Resource No.", ResourceCode)` call can now `Error()` (via table_50610's mandatory-skill check above) with no handling, which would propagate raw/unstyled through the DHTMLX JS↔AL bridge. Fixed by wrapping it in a new `[TryFunction] local procedure TryValidateRequestedResourceNo(var DayPlanning: Record "Day Planning"; ResourceCode: Code[20])` (page-level, placed next to `ClearResourcePanelFilter()`/other local procedures) and applying `if not TryValidateRequestedResourceNo(...) then begin Message('Cannot add this Day Planning: %1', GetLastErrorText()); exit; end;` — surfaces the actual server-side error text (not a re-hardcoded duplicate message) inside a clean `Message()` dialog instead of a raw error. **This exact "if not BooleanFn() then Message(...)" idiom is this page's OWN established convention** for JS-invoked trigger failures — already used verbatim at `OnLinkCreated` (`if not LinkHandler.UpsertLinkFromJson(linkData) then Message('Failed to save link...')`, ~line 320) and as an inline guard-clause in `onOpenResourceScheduler` (`if StrPos(resourceId,'-') = 0 then begin Message(...); exit; end;`, ~line 435). **Do NOT use the `[TryFunction]` style seen in `src/dhx/ganttdemo2/DateEngine/*.al`** as precedent for this page — per the ID-range memory note above, that folder is a hand-rolled test harness explicitly flagged "ignore as a pattern," unrelated to the live Gantt UI's own conventions; `[TryFunction]` itself is standard AL (not borrowed from DateEngine), only its combination with the `if not X then Message(...)` surfacing idiom is what's actually this page's convention.

**`report_50600_RepairDayPlanningResourceGroup.al` (report 50600 "RepairData") — first real implementation (2026-07-09):** Was a dead stub (`n: Integer` declared, never assigned, `dataset {}` empty) with a stale comment elsewhere in `codeunit_50602` referencing a non-existent `RepairDemoResourceCapacityRegenerate` procedure supposedly living here (still doesn't exist — not implemented, out of scope until asked). First real logic added: `OnPreReport()` now calls `Codeunit "Create Demo Data"`'s `Initialize()` then `CreateDemoCalendar()` to backfill/refresh just the DEMOCAL Base Calendar + its Base Calendar Change rows (Dutch holidays + 36 monthly custom-days-off) against ALREADY-EXISTING demo data, without re-running the full `OnRun()` (which would wipe/regenerate every job/resource/day planning). To support this, two `codeunit_50602` procedures went from `local` to public — **exactly these two, no wider refactor**: `Initialize()` (required first — populates `gStartDate`/`gEndDate`/`gLogEntryNo`, which `CreateDemoCalendar()`/`CreateDemoCalendarChanges()`/`LogRecord()` all depend on; resumes `gLogEntryNo` from the last existing log entry rather than restarting at 0, avoiding a PK collision with a prior full run's rows) and `CreateDemoCalendar(): Code[10]` (does the actual idempotent Base Calendar + calendar-changes work). This matches the established "some procedures are public specifically for external repair-report reuse" convention already used by `GetDemoDateWindow`/`GetUniqueDemoResourceName`/`GetRandomDailyCapacity`/`IsDemoWorkingDay`/`GetUniqueDemoJobName`. The report's `Permissions` property gained `tabledata "Base Calendar" = rimd`, `tabledata "Base Calendar Change" = rimd`, `tabledata "Demo Data Log Entry" = rimd` (existing `Day Planning`/`Resource`/`Res. Capacity Entry`/`Work-Hour Template` entries left untouched — presumably for other not-yet-implemented repair actions in this same report). The reported repair count (`n`) is a `Demo Data Log Entry` row-count delta (before/after), not a dedicated return value — works because every insert this touches already goes through `LogRecord()` and idempotent skips don't increment the log, so a rerun against unchanged data correctly reports `n = 0`. Minor known imprecision: the delta technically includes the Base Calendar's own 1 log row (from `CreateDemoCalendar()` itself) in addition to the Base Calendar Change rows, despite the message text saying "Base Calendar Change record(s)" — cosmetic, not fixed since it matches the exact spec given for this task.

**General technique — decompiling a base-app table when `al_symbolsearch` can't answer the question (established 2026-07-09):** `al_symbolsearch` only exposes field signatures (e.g. `"Recurring System": Option`), never Option `OptionMembers`/values, trigger bodies, or other implementation detail for base-app (non-project) objects. When that detail is actually needed (exact option ordinals, validation/trigger side effects, etc.), the on-prem symbol `.app` files under `.alpackages/` are real ZIP archives containing the FULL AL SOURCE under a `src/` folder (Microsoft ships Base Application as source, not just compiled symbols) — extract with `unzip` (works despite a ~40-byte non-standard header prefix BC adds before the `PK\x03\x04` signature; `unzip -o` tolerates it with a warning) and `find`/`grep` the `src/` tree for the object file, e.g. `src/Foundation/Calendar/BaseCalendarChange.Table.al`. This is strictly more reliable than guessing OptionMembers/trigger behavior from the field's Caption or from general BC knowledge — always do this before finalizing code that depends on undocumented base-app Option values or trigger side effects, per this project's "verify via symbol tools, don't guess" standing instruction. Clean up the extracted temp folder afterward (used `C:\Users\Ahmad\AppData\Local\Temp\claude\bcbaseapp_extract` this time, then `rm -rf`'d it).

**`IsWorkingDay()` now honors DEMOCAL's calendar exceptions (added 2026-07-09).** `OnRun()`'s call order was fixed so `CreateDailyOptimizerSetupDefault()` (creates DEMOCAL + its Base Calendar Change exceptions) runs BEFORE `CreateCapacityAndDayPlanning()` (was previously last, after capacity/day-planning generation, which meant DEMOCAL didn't exist yet when `IsWorkingDay()` needed it). `IsWorkingDay(DT)` still checks the Work-Hour Template weekday flags first as a fast-path/short-circuit (unchanged), then ALSO checks DEMOCAL's exceptions via `Codeunit "Calendar Management".IsNonworkingDay(DT, gCustomizedCalendarChange)`. New `EnsureBaseCalendarLoaded()` loads `gBaseCalendar`/calls `CalendarMgt.SetSource(gBaseCalendar, gCustomizedCalendarChange)` ONCE (memoized via `gBaseCalendarLoaded`, same convention as `gWorkHourTemplateLoaded`/`gResourceSkillCache`/`gResourcePoolCache`/`gNamesLoaded`) rather than per-call, because `IsWorkingDay()` runs tens of thousands of times (once per resource per candidate day across the ~2-year demo window) vs. `ExpectedWeekDay` in codeunit 50610 (which re-runs `SetSource` per call since it's called far less often — that pattern was NOT copied here for performance reasons). Verified via `al_symbolsearch` that `Calendar Management.IsNonworkingDay(TargetDate; var CustomizedCalendarChange)` only reads/filters the already-combined record by date per call (not a cursor/stateful API), so calling `SetSource` once and reusing the same var record across many `IsNonworkingDay` calls is safe and is the standard base-app pattern. Also confirmed `gCustomizedCalendarChange` should be declared as a plain (non-`temporary`) `Record "Customized Calendar Change"` — matches how codeunit_50610's `ExpectedWeekDay` declares its own local `CustomizedCalendarChange` variable, so this isn't a new/unproven declaration style. If `Daily Optimizer Setup` doesn't exist yet or `"Base Calendar"` is blank/invalid, `EnsureBaseCalendarLoaded()` just leaves `gCustomizedCalendarChange` unpopulated and returns — `IsNonworkingDay()` against an empty record safely returns false (no exceptions found), so `IsWorkingDay()` never crashes in that edge case. `IsDemoWorkingDay()` (the public wrapper used by the external repair report) calls `IsWorkingDay()` internally, so it inherits this fix with no separate change needed.
**How to apply:** If any other per-date-loop procedure in this codeunit needs calendar-exception awareness, reuse `EnsureBaseCalendarLoaded()`/`gCustomizedCalendarChange` rather than adding a fresh `SetSource` call inline — the memoization only pays off if every caller shares the same cached record.

**Work Order / Order Intake schema — object names differ from their captions (learned 2026-07-09):** Table 50604's actual AL object name is `"Order Intake Header Opt."` (Caption = "Order Intake Header") — searching/referencing it as `"Order Intake Header"` in code fails; always use the `Opt.`-suffixed name. Table 50608 is `"Work Order"` (file `table_50608_Workorder.al`), PK `"Work Order No."`. Enum 50612 `"Workload Source Type"`: `Manual` (0), `"Order Intake"` (1). Enum 50607 `"DayPlanning Order Intake Status"`: `Open` (0), `Ready` (1), `Released` (2), `Done` (3). **Trap:** `"Order Intake Header Opt."`'s `OnInsert` trigger unconditionally runs `"Order Date" := Today();` as its last line, regardless of what the caller set beforehand and regardless of whether the No.-auto-numbering branch fired — so any code that wants a specific `"Order Date"` (e.g. demo data wanting a date inside a rolling window, not literally today) MUST set it via `Modify()` AFTER `Insert(true)`, never rely on the pre-Insert value sticking. `"Work Order"`'s `OnInsert` has no such trap (just audit fields `Created DateTime`/`Created By` + `SetLastModified()`). Both tables' PK fields (`"No."` / `"Work Order No."`) skip their No.-Series auto-numbering branch entirely as long as the caller sets that field to a non-blank fixed code before `Insert()` — this is how `CreateDemoData` uses fixed `DOI*`/`DWO*` codes without touching `"Order Intake Nos"`/`"Work Order NOS"` No. Series.

**`CreateWorkOrderDemoData()` (added 2026-07-09):** New top-level procedure in `codeunit_50602_CreateDemoData.al`, called from `OnRun()` last (after `CreateGanttChartSetupDefaults()`). Builds one dedicated Job whose `"No."` equals the shared demo Customer's `"No."` (mirrors this app's real Work-Order→Job convention), with 18 Posting Job Tasks in two `Begin-Total`/`End-Total` groups under Job Tasks `'1000'..'1999'` and `'2000'..'2999'` (`BuildWorkOrderDemoTasks`, same `AddTask`/`Indent.IndentJobTasks` pattern as `BuildTasksJOB001`). Group 1 (`'1010'..'1100'`, 10 tasks) is Work-Order-linked: single-day exact-hours tasks (2h/3h/5h) and 2-3 day span tasks, all dates snapped forward via new `NextWorkingDayOnOrAfter(CandidateDate)` (loops `IsWorkingDay()`, so it automatically respects DEMOCAL's calendar exceptions from the fix above). Group 2 (`'2010'..'2080'`, 8 tasks) is plain — reuses `BuildDayPlanningsForTask` directly per-task (NOT `BuildDayPlanningsForJob` for the whole job, which would double-generate/conflict with Group 1's custom rows via its blanket `Job Task Type::Posting` filter). 3 Order Intakes (`DOI0001..DOI0003`, all `Status::Open`) and 10 Work Orders (`DWO0001..DWO0010`, round-robin WO1-4→OI1/WO5-7→OI2/WO8-10→OI3, `"Source Type"::"Order Intake"`, `"Project No."`/`"Project Task No."` linking back to the Job/Job Task, `Description` copied from the matching Job Task's `Description`) are created via new idempotent `UpsertOrderIntake`/`UpsertWorkOrder` helpers. New `InsertWorkOrderDayPlanning(JT, ResNo, DT, Hours, WorkOrderNo)` is a dedicated Day Planning inserter for Group 1 (mirrors `InsertLeaderDayPlanning`'s field-by-field shape but takes an exact `Hours` value instead of `GetTimeSlot()`'s weekday pattern, and sets `"Work Order No."` — the one existing-but-previously-unused Day Planning field this whole feature exists to populate). All new records (Job, 24 Job Tasks, 3 Order Intakes, 10 Work Orders, all Day Planning rows) go through `LogRecord`/`UpsertJob`/`AddTask`'s existing internal `LogRecord` calls, so `DeleteDemoData()` wipes and recreates everything fresh each run.
**How to apply:** If this Job/task structure needs a 3rd group or more Work Orders later, follow the same `TaskNos: array[N] of Code[20]` + `case`/loop shape already used in `CreateWorkOrderDemoWorkOrders`/`CreateWorkOrderLinkedDayPlannings`/`CreateWorkOrderNonLinkedDayPlannings` rather than hand-duplicating each call — it's already the established shape for "N fixed demo sub-records mapped 1:1 to N fixed Job Task Nos" in this procedure group.

**Day-Planning-to-Invoice Release 1 (added 2026-07-13):** Turns posted Day Planning resource usage (Job Ledger Entry) into billable Job Planning Lines. New objects: tableextension 50609 "Opt. Skill Code" (`src/tableext/tableext_50609_SkillCode.al`, extends standard table 5955 "Skill Code") adds field 50609 "Invoice Resource No." (Code[20], TableRelation Resource."No."). Table 50614 "Job Ledger Invoice Link" (`src/table/table_50614_JobLedgerInvoiceLink.al`) — traceability link (see key-redesign entry below for CURRENT key shape, which superseded the original PK-on-"Job Ledger Entry No." design described when this feature was first built). Codeunit 50607 "Job Invoice Prep. Mgt." (`src/codeunit/codeunit_50607_JobInvoicePrepMgt.al`), single entry point `PrepareInvoiceLines(JobNo, JobTaskNo): Integer`. Report 50606 "Job Planning Line Usage Detail" (`src/report/report_50606_JobPlanningLineUsageDetail.al`) — given Job Planning Line(s), lists the linked posted usage plus a per-Resource hours summary.
- **Candidate selection:** `Job Ledger Entry` where `"Entry Type"=Usage`, `Type=Resource`, `"Opt. DayPlanning Line No." <> 0` (i.e. only usage that actually traces back to a Day Planning row — usage posted through some other route has no Skill to resolve and is silently out of scope, not error'd), and not already present in `Job Ledger Invoice Link` (`Get()` by PK).
- **Grouping key (open design-doc decision, resolved):** Job No. + Job Task No. + Skill's `Invoice Resource No.` + Unit of Measure Code. Deliberately NOT grouped by Unit Price — Unit Price is left to standard BC resource price-list resolution, triggered once per group by replaying the same field-`Validate()` order a manual Job Planning Lines entry would use: `Insert()` (Job No./Job Task No./Line No. only) → `Validate("Line Type", Billable)` → `Validate(Type, Resource)` → `Validate("No.", InvoiceResNo)` → `Validate("Unit of Measure Code", ...)` only if it differs from the resource's own default → `Validate(Quantity, SummedHours)` → `Modify(true)`. This order was reverse-engineered by decompiling `Microsoft_Base Application_28.2.....app`'s `src/Projects/Project/Planning/JobPlanningLine.Table.al` (see the general decompile technique noted elsewhere in this file) — `Validate("No.")` internally calls `CopyFromResource()` which calls `Validate("Unit of Measure Code", Res."Base Unit of Measure")`, and both `"No."` and `"Unit of Measure Code"` validation end by calling `Validate(Quantity)`, which is what actually triggers `UpdateAllAmounts()`/price resolution — replaying this exact sequence is what makes the created line behave identically to a hand-typed one.
- **Per-entry Skill Code, not per-group:** `Job Ledger Invoice Link."Skill Code"` is stored per linked usage entry (not once per created Job Planning Line), because two different Skills can share the same `Invoice Resource No.` and thus land in the same group/line — storing it on the link row (which the design doc already flagged "optional, for reporting only") is the only place it stays accurate.
- **Atomicity:** two-pass — pass 1 does pure validation/grouping with zero DB writes (so a Skill missing `Invoice Resource No.` raises `Error()` before anything is created); pass 2 writes. No `COMMIT()` anywhere in the codeunit, so a mid-pass-2 runtime error rolls back the *entire* run via BC's ambient per-interaction transaction (the caller is a page action) — deliberately not using an explicit transaction/try-wrapper since the platform already provides all-or-nothing here.
- **Implementation gotcha, ORIGINAL approach then SUPERSEDED (2026-07-13, see fix entry below):** the per-group list of included Job Ledger Entry Nos. was originally accumulated as a comma-joined `Text` (`Dictionary of [Text, Text]`) rather than `Dictionary of [Text, List of [Integer]]`, out of an unverified worry that `List of [T]` being a reference type would let one shared local `List` variable alias across groups. This Text round-trip (`Format`/`Split(',')`/unchecked `Evaluate`) was later found to be a real bug risk (unchecked `Evaluate` boolean return could silently write a stale `EntryNo` into `Job Ledger Invoice Link`) and was replaced — see below.
- **Delete-subscriber location:** `codeunit_50603_EventSubs.al` (existing convention, not a new codeunit) gained `[EventSubscriber(Table::"Job Planning Line", 'OnAfterDeleteEvent')]` — deletes matching `Job Ledger Invoice Link` rows (filtered Job No./Job Task No./Invoice Job Planning Line No.), skips `IsTemporary` records, `DeleteAll(false)` (no triggers on the link table). Codeunit's `Permissions` gained `tabledata "Job Ledger Invoice Link" = rd`.
- **UI entry point:** `Pag50662.WorkorderCard.al` gained action `PrepareInvoiceLines` (Processing area, Promoted/PromotedIsBig/PromotedCategory=Process, `Image = Invoice`) right before the existing `GanttChartDHX` action — calls the codeunit for `Rec."Project No."`/`Rec."Project Task No."`, `CurrPage.Update()`, then a `Message()` summarizing lines created.
- **Report 50606 deviation from this project's usual report style:** the project's other reports are either `ProcessingOnly = true` batch jobs (50600/50601/50660) or have a full custom RDLC layout in a dedicated subfolder (50602–50605, e.g. `Rep50603.DayPlanningDetails.al`). 50606 is `ProcessingOnly = false` with NO `rendering`/RDLC section at all — relies on BC's auto-generated default layout, since authoring a real RDLC by hand was out of scope for "keep it simple" in the Release 1 brief. Structure: top dataitem Job Planning Line → nested `Job Ledger Invoice Link` (via `DataItemLink` on Job No./Job Task No./Invoice Job Planning Line No., looks up the actual `Job Ledger Entry` per row) → sibling nested `Integer` dataitem (`ResourceSummaryLoop`, the standard AL "no physical buffer table" summary idiom: `SetRange(Number, 1, Count); if Count=0 then CurrReport.Break();`, indexing into `List of [Code[20]]` + 2 Dictionaries built during the sibling dataitem's `OnAfterGetRecord`). The summary reset (`ClearResourceSummary()`) had to go in the **parent** Job Planning Line dataitem's `OnAfterGetRecord` (fires once per parent record, before its children iterate) — putting it in `OnPreDataItem` instead (which fires once for the whole report) was an early mistake caught and fixed before build.
**How to apply:** If Release 2 adds Item/G/L usage types, extend the candidate filter (`Type=Resource` currently hardcoded) and add a parallel `CopyFrom*`-appropriate `Validate()` sequence per type — the base-app decompile technique + field-validate-order approach documented above generalizes directly.

**Second entry point (added 2026-07-13, same Release 1 feature):** `page_50630_DayPlannings.al`'s previously-stub `CreateSalesInvoice` action (list page 50630 "Day Plannings", `SourceTable = "Day Planning"`) was renamed/reimplemented as `PrepareInvoiceLines` ("Prepare Invoice Lines" — same caption as the Work Order card action) and now calls the SAME codeunit 50607 — no second/parallel invoicing codeunit was built. Also renamed the matching `actionref` in the promoted `Invoicing` group (`PrepareInvoiceLines_Promoted`) and dropped the stray `Ellipsis = true` (leftover from the old stub — this action shows no dialog). Since this list page lets a user multi-select Day Planning rows spanning several Job No./Job Task No. combinations, the action calls `CurrPage.SetSelectionFilter(SelectedDayPlanning)` (plain non-temporary `Record "Day Planning"` var — matches this project's existing `SetSelectionFilter` convention, e.g. `Pag50601.OptiResourceList.al`/`Pag50641.OptiJobList.al`), derives the distinct (Job No., Job Task No.) pairs via a `Dictionary`-backed dedupe (same `"%1|%2"` text-key idiom as codeunit 50607's own grouping), and calls the invoice-prep codeunit once per distinct pair.
- **New codeunit 50607 procedure:** `[TryFunction] TryPrepareInvoiceLines(JobNo; JobTaskNo; var LinesCreated: Integer)` — thin wrapper around `PrepareInvoiceLines`. Exists specifically so a multi-job-pair batch can catch a per-pair hard `Error()` (typically "Skill has no Invoice Resource No.") without aborting the whole run: AL's `TryFunction` mechanism gives each call its own rollback checkpoint, so a failing pair's partial writes roll back while EARLIER pairs in the same batch (already-completed, separate `TryFunction` calls) stay committed. This composes cleanly with `PrepareInvoiceLines`'s own no-`COMMIT()`/two-pass atomicity design (documented above) without needing any change to that procedure.
- **Reporting convention:** collect-and-report, not stop-on-first — the action loops all distinct pairs, accumulates `TotalLinesCreated` for successes and a `\`-joined failure list (`Job %1, Job Task %2: %3` using `GetLastErrorText()`) for failures, then shows one `Message()` with both. Matches the Work Order card action's "summarize lines created" style but extended for the multi-pair case.

**UI for `"Invoice Resource No."` on Skill Code (added 2026-07-13, same Day-Planning-to-Invoice feature):** tableextension 50609 "Opt. Skill Code" (`src/tableext/tableext_50609_SkillCode.al`) added field 50609 "Invoice Resource No." with no UI at first. New pageextension 50626 "Skill Codes Opt." (`src/pageext/pageext_50626_SkillCodes.al`) extends the STANDARD base-app page 6018 "Skill Codes" (namespace `Microsoft.Service.Setup`) — that page's repeater is `Control1` with only `Code`/`Description`; the new field is added via `addafter(Description)`. **Deliberately did NOT use `src/page/Pag50646.OptiSkillCodes.al`** ("Opti Skill Codes") even though its caption is also "Skill Codes" — that page is `SourceTableTemporary = true`, a temp lookup/picker populated via `SetTempSkill` from `table_50612_ResourceWeeklyHours.al`'s `HandOverToPage` (called from `Pag50626.SummaryView.al`); edits there never persist to real Skill Code records. This is the same "two similarly-named pages, only one is real master data" trap as the earlier `"Order Intake Header Opt."` naming note — always confirm `SourceTableTemporary`/actual master-data page via `al_symbolsearch` (or reading the page file) before extending, don't trust the caption alone.
- **Pageextension ID gotcha:** `src/pageext/pageext_xxx_ResourceSkills.al` has a placeholder "xxx" in its FILENAME but the object itself is already `pageextension 50607 "ResourceSkills Opt."` — a plain filename glob for numeric IDs would miss that 50607 is taken. Always cross-check via `al_symbolsearch` (kinds=PageExtension, scope=project) not just filenames when picking the next free ID. As of 2026-07-13, pageextension IDs in use: 50600-50607, 50610, 50613, 50620-50625 (50624 lives under `src/dhx/orderintakekanban/`), 50626 (new). Free gaps still remain at 50608, 50609, 50611, 50612, 50614-50619 — don't assume 50627 is next without reverifying.

**Base app already ships an embeddable Job Planning Line list part (learned 2026-07-10):** Standard page `"Job Planning Lines Part"` (namespace `Microsoft.Projects.Project.Planning`) is `PageType = ListPart`, `SourceTable = "Job Planning Line"`, caption "Project Planning Lines" (label translation in this environment renders "Job" as "Project" everywhere, consistent with `"Job No."` showing as "Project No." etc. throughout this app). Wired into `Pag50662.WorkorderCard.al` as a new `group(ProjectPlanningLinesGroup) { Caption = 'Project Planning Lines'; part(ProjectPlanningLines; "Job Planning Lines Part") { SubPageLink = "Job No." = FIELD("Project No."), "Job Task No." = FIELD("Project Task No."); } }` fasttab, placed right after the existing `part("Day Plannings"; "Work Order Day Plannings")`. Confirmed via `al_symbolsearch` (kinds=Page, scope=all) before using it — don't assume a base-app part page exists from caption/name alone, always verify with symbolsearch first since guessing wrong here would mean building a redundant custom ListPart.
**How to apply:** Before creating a new custom ListPart page to embed a standard BC table (Job Planning Line, Sales Line, etc.) on a card page, `al_symbolsearch` for an existing `*Part`/`*ListPart` page over that table first — Microsoft ships many of these for exactly this embedding use case. Note: a `group` wrapper and its inner `part` control cannot share the same AL control name (AL0155 duplicate-member error) — name the group distinctly (e.g. `XxxGroup`) even when the part control's name matches the FastTab's conceptual purpose.

**Table 50614 "Job Ledger Invoice Link" — key redesign + 2 fallout bugs fixed (2026-07-13):** User manually restructured this table's keys via the IDE (not done by an agent): PK moved from `"Job Ledger Entry No."` itself to a new surrogate `field(1; "Entry No."; Integer)`, with the other fields renumbered to 10/20/30/40/50. Also added `field(35; "Day Line No."; Integer)` (TableRelation to `"Day Planning"."Day Line No." where("Job No."=field("Job No."), "Job Task No."=field("Job Task No."))`, Editable=false) so a link row can be traced back to its exact originating Day Planning row, not just Job No./Job Task No. Two bugs found and fixed as fallout from the PK change:
1. **`field(1; "Entry No."` had no `AutoIncrement = true`** — every `Insert(true)` would have tried `Entry No. = 0`, PK-violating on the 2nd insert. Fixed by adding `AutoIncrement = true`.
2. **The anti-double-invoicing guard was silently gone.** The table's original design relied on the PK itself (on `"Job Ledger Entry No."`) to block a second link row per usage entry. Once the PK became the surrogate `Entry No.`, nothing enforced that anymore, AND `codeunit_50607_JobInvoicePrepMgt.al`'s duplicate-check (`if not JobLedgerInvoiceLink.Get(JobLedgerEntry."Entry No.")`) was now silently wrong too — `.Get()` looked up by the NEW PK (unrelated surrogate) instead of `"Job Ledger Entry No."`, so it would almost never find an existing link (false negative, real double-invoicing risk). Fixed with two changes together: added `key(JobLedgerEntryNo; "Job Ledger Entry No.") { Unique = true; }` to the table (this is now the ACTUAL double-invoicing guard, not the PK), and changed the codeunit's check to `JobLedgerInvoiceLink.SetRange("Job Ledger Entry No.", EntryNo); if JobLedgerInvoiceLink.IsEmpty() then ...` instead of `.Get()`.
- Also threaded a new `EntryDayLineNo: Dictionary of [Integer, Integer]` through `PrepareInvoiceLines`/`CreateInvoicePlanningLine` (same pattern as the pre-existing `EntrySkillCode` dictionary) so `"Day Line No."` gets populated on each link row from `JobLedgerEntry."Opt. DayPlanning Line No."` at insert time.
- Invariant confirmed still holding (no code change needed): `"Job Ledger Entry No."` (field 10) and `Day Planning."Job Entry No."` (table 50610 field 151) are guaranteed equal for the same originating row because both are populated from the same `Job Ledger Entry."Entry No."` at posting time (see `codeunit_50603_EventSubs.al`'s `UpdateDayPlanningAfterJobLedgEntryInsert`) — holds as long as codeunit 50607 keeps resolving Day Planning via `JobLedgerEntry."Opt. DayPlanning Line No."` as it already does.
- Page 50681 "Job Ledger Invoice Link" list page updated to show both `"Entry No."` (now with a ToolTip) and the new `"Day Line No."` field.
**How to apply:** If this table's keys are touched again, remember the double-invoicing guard now lives on the `JobLedgerEntryNo` Unique key, NOT the PK — don't let a future refactor silently drop that key while assuming the PK still protects it. Any `.Get()` call against this table anywhere else in the codebase should be re-checked for the same "used to be PK, now isn't" trap before trusting it.

**CORRECTION (2026-07-13, later same day): the key redesign above was REVERTED by the user via the IDE.** Table 50614 is back to its ORIGINAL simple shape: `field(1; "Job Ledger Entry No."; Integer)` as the (only) PK/Clustered key, followed by `field(2; "Job No.")`, `field(3; "Job Task No.")`, `field(4; "Invoice Job Planning Line No.")`, `field(5; "Skill Code")`. **The surrogate `"Entry No." AutoIncrement` PK, the `JobLedgerEntryNo` Unique key, and the `"Day Line No."` field are ALL GONE again** — do not act on the "key redesign + 2 fallout bugs fixed" entry above as current state; it only describes an intermediate design that no longer exists. The double-invoicing guard is once again simply the PK on `"Job Ledger Entry No."` itself (a usage entry can only ever be linked to one invoice planning line, ever — don't weaken this with a composite key).
- `codeunit_50607_JobInvoicePrepMgt.al` was correspondingly reworked: Pass 1 of `PrepareInvoiceLines` now loops **`Day Planning`** directly (`SetRange("Job No."/"Job Task No."); SetRange(Posted, true)`) instead of `Job Ledger Entry`, per explicit user instruction that `Day Planning."Job Entry No."` (table 50610 field 151) — populated by `codeunit_50603_EventSubs.al`'s `UpdateDayPlanningAfterJobLedgEntryInsert` at posting time — should be the literal SOURCE of `Job Ledger Invoice Link."Job Ledger Entry No."`, not an independently-looped `Job Ledger Entry."Entry No."` relying on an unenforced coincidental equality. For each Day Planning row with `"Job Entry No." <> 0` and not already linked (`JobLedgerInvoiceLink.SetRange("Job Ledger Entry No.", DayPlanning."Job Entry No."); IsEmpty()`), the actual `Job Ledger Entry` is still fetched via `.Get(DayPlanning."Job Entry No.")` (guarded, silently skipped if missing) purely to read `Quantity`/`"Unit of Measure Code"` and to scope to `Entry Type=Usage`/`Type=Resource`. The `EntryDayLineNo` dictionary and `CreateInvoicePlanningLine`'s extra parameter for it are REMOVED (fed a field that no longer exists); signature is back to `(JobNo, JobTaskNo, InvoiceResNo, UOMCode, Hours, EntryNosText, EntrySkillCode)`.
- Page 50681 was NOT changed this round — it already only has the 5 real fields (`"Job Ledger Entry No."`, `"Job No."`, `"Job Task No."`, `"Invoice Job Planning Line No."`, `"Skill Code"`); it never had `"Entry No."`/`"Day Line No."` controls added in the first place, so nothing to remove.
**How to apply:** Trust the table file's actual current field list over either historical memory entry above before making further changes here — this table's key design has now flip-flopped twice in one day. If asked to touch it again, re-read `src/table/table_50614_JobLedgerInvoiceLink.al` fresh rather than relying on either memory entry's field numbering.

**Selection-scoping fix for page 50630's `PrepareInvoiceLines` action (added 2026-07-13, same Day-Planning-to-Invoice feature).** Bug: the list-page multi-select action derived distinct (Job No., Job Task No.) pairs from the user's selection, then called `TryPrepareInvoiceLines(JobNo, JobTaskNo, ...)` per pair — that procedure does its own broad `Day Planning` sweep (`SetRange` Job No./Job Task No./Posted=true), so selecting 3 of 50 posted rows in a job/task still invoiced all 50; the actual `"Job Entry No."` values of the user's selection never entered the decision, only the Job/Task pair did.
- **Codeunit 50607 refactor:** the old `PrepareInvoiceLines(JobNo, JobTaskNo)` body (Pass 1/Pass 2 loop) was extracted into `local procedure PrepareInvoiceLinesFromDayPlanning(var DayPlanning: Record "Day Planning") LinesCreated: Integer`, which no longer builds its own `SetRange`s — it just `FindSet()`/`Next()`s whatever recordset the caller already filtered. Two public entry points now front it: `PrepareInvoiceLines(JobNo, JobTaskNo)` (unchanged signature/behavior — builds the same Job No./Job Task No./Posted=true `SetRange`s itself, then delegates) and new `PrepareInvoiceLinesForSelection(var SelectedDayPlanning: Record "Day Planning")` (delegates directly, no `SetRange`s added — trusts the caller's filter as-is). A matching `[TryFunction] TryPrepareInvoiceLinesForSelection(var SelectedDayPlanning; var LinesCreated: Integer)` wrapper was added alongside the pre-existing `TryPrepareInvoiceLines`, same per-pair rollback-isolation rationale. Added defense-in-depth inside the loop: `if DayPlanning.Posted and (DayPlanning."Job Entry No." <> 0) then` (was just the `"Job Entry No." <> 0` check) — the Work Order card path still pre-filters `Posted=true` via `SetRange`, but the selection path could receive selected-but-not-yet-posted rows, which are now silently skipped (same "not a qualifying candidate" semantics as the Skill checks) rather than treated as errors.
- **Page 50630's `PrepareInvoiceLines` action:** kept the existing dedup-into-distinct-pairs loop (still needed — it's what isolates "one bad job's Skill error doesn't block other jobs in the same batch"), but per pair now does `SelectedDayPlanning.SetRange("Job No.", ...); SelectedDayPlanning.SetRange("Job Task No.", ...)` (narrowing the SAME var that `CurrPage.SetSelectionFilter` populated once before the dedup loop, further restricting it to just that pair's selected rows) and calls `JobInvoicePrepMgt.TryPrepareInvoiceLinesForSelection(SelectedDayPlanning, LinesCreated)` instead of the broad-sweep `TryPrepareInvoiceLines(JobNo, JobTaskNo, ...)`.
- Work Order card's action (`Pag50662.WorkorderCard.al`) is UNCHANGED and still intentionally broad-sweep via `PrepareInvoiceLines(JobNo, JobTaskNo)` — "invoice everything ready for this job task" is the correct semantics there, only the list-page multi-select action needed selection-scoping.
**How to apply:** If a third caller needs invoice-prep over a caller-supplied recordset (not a full Job/Task sweep), reuse `PrepareInvoiceLinesForSelection`/`TryPrepareInvoiceLinesForSelection` rather than adding another bespoke `SetRange`-then-sweep entry point.

**Fixed the unchecked-`Evaluate` Text round-trip in `codeunit_50607_JobInvoicePrepMgt.al` (2026-07-13), replaced with `Dictionary of [Text, List of [Integer]]`.** Pass 1's `GroupEntryNosText: Dictionary of [Text, Text]` (comma-joined `Format(DayPlanning."Job Entry No.")` strings) and Pass 2's `EntryNosText.Split(',')` + `Evaluate(EntryNo, EntryNoText)` (return value never checked) were removed entirely — `DayPlanning."Job Entry No."` now stays a typed `Integer` end-to-end into `Job Ledger Invoice Link."Job Ledger Entry No."`, no Format/Split/Evaluate anywhere in this flow. New var is `GroupEntryNos: Dictionary of [Text, List of [Integer]]`; `CreateInvoicePlanningLine`'s parameter changed from `EntryNosText: Text` to `EntryNos: List of [Integer]`, its body is now a plain `foreach EntryNo in EntryNos do begin ... end;`.
- **The safe Dictionary-of-List idiom used (worth reusing anywhere else in this codebase a `Dictionary of [Text, List of [T]]` grouping pattern is needed):** never assume `Dictionary.Get()` on a List-valued entry hands back something mutable-in-place — always `Get()` into a local var, mutate the local copy, then explicit `Set()` back into the dictionary. Two local vars: `TempEmptyIntList` (used only at group-creation time, immediately before `GroupEntryNos.Add(GroupKey, TempEmptyIntList)`, reset via the **global `Clear(Var)` procedure** — `Clear(TempEmptyIntList)`, NOT a `.Clear()` method call, which `List of [T]` doesn't have — so it's a genuinely fresh empty list every time, not a possibly-shared object) and `TempIntList` (used on every touch, new-group or pre-existing: `TempIntList := GroupEntryNos.Get(GroupKey); TempIntList.Add(...); GroupEntryNos.Set(GroupKey, TempIntList);`, unconditionally, outside the new-group `if` block). This is what actually resolves the earlier entry's aliasing worry — the original Text-round-trip workaround is no longer needed as a pattern anywhere else in this codebase.
**How to apply:** If a future feature needs a per-group/per-key accumulating list again, use this `Dictionary of [Text, List of [T]]` + `Clear()`-then-`Add()` + `Get()`-mutate-`Set()`-back pattern directly rather than reinventing the Text-join workaround "for safety" — the aliasing risk that motivated the Text workaround is fully addressed by this idiom.

**Breakdown counters on `PrepareInvoiceLines`/`PrepareInvoiceLinesForSelection` (added 2026-07-13, same Day-Planning-to-Invoice feature).** User complaint: running "Prepare Invoice Lines" on a filtered selection that matched nothing returned only "0 Project Planning Line(s) created." with zero explanation of why. Fixed by adding 4 `var` out-counters, threaded through the whole call chain (`PrepareInvoiceLinesFromDayPlanning` → `PrepareInvoiceLines`/`PrepareInvoiceLinesForSelection` → `TryPrepareInvoiceLines`/`TryPrepareInvoiceLinesForSelection`): `ProcessedCount` (row reached the success path, right before the pre-existing `EntrySkillCode`/`GroupEntryNos` population — incremented at the very end of that inner `begin...end` block), `AlreadyLinkedCount` (`JobLedgerInvoiceLink.Get(DayPlanning."Job Entry No.")` succeeds), `NotPostedCount` (outer `DayPlanning.Posted and ("Job Entry No." <> 0)` guard is false), `SkippedOtherCount` (lumps together the two rare edge cases: `JobLedgerEntry.Get(...)` fails, or it succeeds but Entry Type/Type isn't Usage/Resource — deliberately one bucket, not two, per the brief). Pass 1's nested `if...then` chain (previously no `else` branches at all) now has an explicit `else` at every level to attribute each excluded row to exactly one bucket.
- **Shared message formatter:** new public `FormatResultMessage(LinesCreated; ProcessedCount; AlreadyLinkedCount; NotPostedCount; SkippedOtherCount): Text` lives on codeunit 50607 itself (not duplicated per-caller) — both `Pag50662.WorkorderCard.al`'s `PrepareInvoiceLines` action and `page_50630_DayPlannings.al`'s `PrepareInvoiceLines` action call it. Always includes the "X record(s) processed into Y Project Planning Line(s)." line (even when X=0, e.g. an all-excluded selection), then conditionally appends (only if >0) an "already in a Project Planning Line" line, a "have not been posted into Job Ledger Entries yet" line, and a "skipped (not eligible resource usage)" line. Falls back to a plain "No records to process." only when ALL FOUR counters are 0. Lines are joined with `\` (BC's native Message()/Error() line-break character), not `TextBuilder`/CRLF.
- **Day Plannings list page's failure path:** on `TryPrepareInvoiceLinesForSelection` returning false, still calls `FormatResultMessage` with whatever counts accumulated before the `Error()` fired (AL's `TryFunction` only rolls back DB writes, not local/var-parameter state — the counters up to the failure point are real and worth showing), then appends `\` + `GetLastErrorText()`-based failure line — so the user sees both "here's what happened before it failed" and "here's why it stopped" in one dialog.
**How to apply:** If a third caller is added later, call `FormatResultMessage` rather than hand-rolling message text again — it's the single source of truth for this wording. If Release 2 adds more skip reasons, prefer folding them into `SkippedOtherCount` unless the user specifically wants a new named bucket (mirrors this session's explicit "lump into one bucket" instruction).

**Dual-write to standard BC `"Job Usage Link"` alongside our own `"Job Ledger Invoice Link"` (added 2026-07-13, same Day-Planning-to-Invoice feature).** Base-app page 92 "Job Ledger Entries" (wired into this app's Role Center as "Project Ledger Entries" earlier the same day) ships a native action "Show Linked Project Planning Lines" that reads from the STANDARD table `"Job Usage Link"` (namespace `Microsoft.Projects.Project.Job`, PK = `"Job No.", "Job Task No.", "Line No.", "Entry No."` — `"Line No."` is the Job Planning Line's, `"Entry No."` is the Job Ledger Entry's) — it does NOT read our custom table 50614 "Job Ledger Invoice Link" at all. There is no dedicated "Job Usage Link Management" codeunit in the base app; base-app code just does plain `Insert()` calls against this table directly wherever it's populated. `codeunit_50607_JobInvoicePrepMgt.al`'s `local procedure CreateInvoicePlanningLine` now dual-writes: for every entry it links via `Job Ledger Invoice Link` (this feature's own traceability row), it ALSO inserts a matching `Job Usage Link` row (`"Job No."`/`"Job Task No."`/`"Line No."` = the new Job Planning Line, `"Entry No."` = the usage entry) — but ONLY when `Job."Apply Usage Link"` is true for that Job, mirroring exactly how native BC gates its own automatic Job Usage Link population (`Job` is fetched once per group call via `Job.Get(JobNo)`, not once per entry). Guarded with `if not JobUsageLink.Get(...) then` before insert (idempotency, matches this codeunit's established "tolerate, don't crash" style) and uses `Insert(true)` (RunTrigger) to run any base-app validation for real. Permissions gained `tabledata "Job" = r` and `tabledata "Job Usage Link" = rim`.
**How to apply:** Standard BC often has its own PARALLEL mechanism for something this app builds a custom equivalent of (Job Usage Link vs. our own Job Ledger Invoice Link is one instance) — before assuming a custom table is the only place a link/trace needs to be written, check whether a base-app page action, factbox, or report already expects a specific standard table to be populated, and dual-write into it gated the same way native BC gates it (here: the Job's own `"Apply Usage Link"` flag). Don't write to `"Job Usage Link"` unconditionally regardless of that flag.

**Generic `"Job Usage Link" OnAfterInsertEvent` subscriber closes the reverse gap (added 2026-07-13, same Day-Planning-to-Invoice feature).** The dual-write above (codeunit 50607 → both tables) only covers OUR OWN code path. Base-app page 92 "Job Ledger Entries" also ships a NATIVE action "Transfer To Planning Lines" (standard `Report "Job Transfer To Planning Lines"`) that creates/reuses Job Planning Lines directly from posted Job Ledger Entries and populates `"Job Usage Link"` itself (same standard mechanism, gated by `Job."Apply Usage Link"`) — completely bypassing codeunit 50607. Run on a Day-Planning-originated entry, this would leave `"Job Ledger Invoice Link"` unaware the entry is already linked (risking a later double-invoice via "Prepare Invoice Lines") and leave report 50606/page 50681 with no traceability row. AL cannot override an existing base-app action's `OnAction` body, so instead of hooking that one action, `codeunit_50603_EventSubs.al` gained a NEW subscriber `JobUsageLink_OnAfterInsertEvent` on `[EventSubscriber(ObjectType::Table, Database::"Job Usage Link", 'OnAfterInsertEvent', '', false, false)]` — this generically catches ANY insert into the standard table (native "Transfer To Planning Lines", codeunit 50607's own dual-write, and any future flow), not just the one action originally reported.
- **Logic:** skips temporary records; exits if `Job Ledger Entry.Get(Rec."Entry No.")` fails or `"Opt. DayPlanning Line No." = 0` (tableextension 50618 "Opt. Job Ledger Entry", field 50619 — not Day-Planning-originated, out of scope). If `Job Ledger Invoice Link.Get(Rec."Entry No.")` (current PK is directly `"Job Ledger Entry No."` — see the key-redesign/revert entries above, do NOT reintroduce a surrogate-PK assumption here) already finds a row, updates it only if Job No./Job Task No./Invoice Job Planning Line No. actually differ from what `Job Usage Link` now says (keeps traceability in sync if native BC retargeted an existing planning line); otherwise inserts a fresh link row, populating `"Skill Code"` from `Day Planning.Get(JobLedgerEntry."Job No.", "Job Task No.", "Opt. DayPlanning Line No.")` if found.
- **Re-entrancy confirmed safe, not just assumed:** codeunit 50607's `CreateInvoicePlanningLine` inserts the `Job Ledger Invoice Link` row FIRST, then the `Job Usage Link` row SECOND (same `JobPlanningLine`/`EntryNo` in both, same call) — verified still true as of this change. So when this subscriber fires off that second insert, `JobLedgerInvoiceLink.Get(Rec."Entry No.")` already succeeds and matches exactly; the update branch is a safe no-op there, never a conflicting double-write.
- **Permissions:** `codeunit_50603_EventSubs.al`'s `Permissions` property changed `tabledata "Day Planning" = m` → `rm` (subscriber reads it) and `tabledata "Job Ledger Invoice Link" = rd` → `rimd` (subscriber inserts/modifies it). No explicit permission added for `"Job Usage Link"` itself — the subscriber only reads fields off the already-provided `Rec` parameter, never its own `Get`/`Find` against that table.
**How to apply:** This is the general pattern for closing a "native BC flow bypasses our custom traceability table" gap — subscribe to the STANDARD table's own `OnAfterInsertEvent` (or similar) rather than trying to hook a specific base-app action/report, since that generically covers every current and future code path that populates the standard table, not just the one a user happened to notice.

**Day Planning period-change reschedule rework (added 2026-07-16), Work-Hour Template resolution + mandatory Base Calendar + cascading shift.** `src/dhx/ganttdemo2/codeunit_50617_DayPlanningPeriodSyncMgt.al`'s `CalculateChanges` was rewritten. Old version read a per-Job-Task weekday pattern from the (now-legacy) `"DayPlanning Week Pattern"` text field and treated Base Calendar as optional (`CalendarLoaded` boolean, silently skipped). New version: resolves the effective Work-Hour Template as Job Task's own tableextension field `"Work Hour Template"` (field 50505, added same session) else `Daily Optimizer Setup."Work hour Template"`; calls `DailyOptimizerSetup.Get(); DailyOptimizerSetup.TestField("Base Calendar")` unconditionally (errors, doesn't skip).
- **New shared helper `codeunit_50610_DayPlanningMgt.al`'s `IsActiveWorkDay(WorkHourTemplateCode: Code[20]; TheDate: Date): Boolean`** (public) — a day is active when the resolved Work-Hour Template has hours > 0 for that weekday (base-app `"Work-Hour Template"` table's Monday..Sunday fields are `Decimal` hours, NOT booleans — confirmed via `al_symbolsearch`, don't assume boolean flags) AND the mandatory Base Calendar doesn't mark it non-working. Blank template code = every weekday active (matches `ExpectedWeekDay`'s existing default). Memoized via new codeunit-instance-scoped vars `gCalendarLoaded`/`gCalCustomizedCalendarChange`/`gCalendarMgt` (Base Calendar loaded once per instance, same convention as `CreateDemoData`'s `EnsureBaseCalendarLoaded()`) and `gCachedWorkHourTemplateCode`/`gCachedWorkHourTemplate`/`gCachedWorkHourTemplateFound` (Work-Hour Template re-fetched only when the code actually changes) — matters here because codeunit 50617 calls this once per candidate date while cascading. Deliberately did NOT reuse the pre-existing `ExpectedWeekDay`/`GetWeekDaysFromDayPlanningGenerator` in the same codeunit (those stay driving `"Day Planning Pattern"`'s own Day1..Day7 booleans for the pattern-generation flow in `CreateDayPlanning` — untouched) since the two active-day concepts (pattern booleans vs. Work-Hour-Template hours) are genuinely different inputs; only the calendar/mandatory-Base-Calendar plumbing is shared.
- **Cascade algorithm (the actual bug fix):** old code computed each existing DayPlanning's `NewDate` via flat offset/clamp then only CLASSIFIED it (Weekend/Public-Holiday/Work-day) for preview — never re-landed it on an active day, never touched subsequent entries. New 3-pass design in `CalculateChanges`: Pass 1 walks affected DayPlanning rows (`SetCurrentKey("Task Date")`) and builds a `Dictionary of [Date, Date]` of distinct-original-date → naive target date via new pure/stateless `local procedure CalculateNaiveNewDate(TaskDate; OldStart; OldEnd; NewStart; NewEnd): Date` (extracted verbatim from the old inline scenario 1.1/1.2/1.3 branches, unchanged arithmetic). Pass 2 walks those distinct dates chronologically, pushing each candidate forward: if its naive target is a genuine NEW distinct value (not equal to the immediately-preceding distinct date's naive value) it's clamped to at least `PrevAdjustedDate + 1` then advanced day-by-day via `IsActiveWorkDay` until active — this is what makes one off-day collision cascade through the rest of the tail. If the naive target is EXACTLY EQUAL to the previous distinct date's naive target (the intentional "clamp several dates onto the same new end date" case from scenarios 1.2B/1.3A when a task is shortened), it deliberately collapses onto the same already-adjusted date rather than being spread apart — this distinction (equal-naive-dates collapse vs. distinct-naive-dates cascade-push) is the key subtlety; getting it wrong would either wrongly spread shrink-clamped entries across multiple days or wrongly collapse genuinely-distinct shifted entries onto one day. Pass 3 rebuilds the preview buffer per DayPlanning row from the `AdjustedDateMap`. Since every date reaching the preview buffer is now guaranteed active by construction, `"Day Type"` is always set to `Work-day` in both Pass 3 and the right-bar-enlarge new-entry loop (Weekend/Public-Holiday enum values still exist, just never produced by this codeunit anymore — no longer meaningful to show the user an off-day for opt-out, since the whole point of the cascade is that off-days are auto-resolved, not offered as a choice).
- **Right-bar-enlarge (new DayPlanning entries beyond the old end date) simplified**: old code walked candidate calendar days and included/showed ANY day matching a manually-parsed `'1|2|3|4|5|6|7'`-style pattern text (including weekends/holidays, for the user to uncheck) — now walks day-by-day and only inserts a preview row when `IsActiveWorkDay` is true, silently skipping inactive days entirely (consistent with Pass 3 now only ever showing active days too). Left-bar-enlarge (extending the start date earlier) still does NOT generate new entries for the newly-added earlier range — this asymmetry pre-existed in the original code (only right-bar-enlarge ever did) and was left as-is; out of scope for this session's cascade fix.
- **Job Task Card path — investigated, decision: leave `tableext_50605_JobTask.al`'s commented-out `DayPlanningPeriodSyncMgt.HandleJobTaskPeriodChange` calls in `PlannedStartDate`/`PlannedEndDate` OnValidate DISABLED (not re-enabled).** Root cause found via `git log -p`: originally enabled (commit `b819680`), then commented out one day later (commit `16eb774`, message "update", inline comment "Change to popup page editor:"). Confirmed via reading `src/report/report_50601_ProjectTaskDateUpdater.al` (report "Job Task Planned Date Updater", opened via page 50618's `PlannedStartDate` field `OnAssistEdit`) that this report's `OnPostReport` ALREADY calls `DayPlanningPeriodSyncMgt.ShowPreview(...)` directly and assigns the new dates via plain field assignment (not `.Validate()`) — so the Card path is fully wired already, just not through the OnValidate trigger. Also: `page_50618_JobTaskCard_Project.al`'s `"Planned Start Date"`/`"Planned End Date"` fields are `Editable = false` (AssistEdit-only) specifically so direct inline edits can't happen — confirmed by reading the page file. Additionally grepped for other `.Validate(PlannedStartDate/PlannedEndDate, ...)` call sites and found two: `codeunit_50602_CreateDemoData.al` (bulk unattended demo-data generation) and `codeunit_50610_DayPlanningMgt.al`'s own `CreateDayPlanning` (pattern-driven Job Task end-date housekeeping sync, called right after generating the very Day Planning lines that date is derived from) — re-enabling the OnValidate hook would make BOTH of these unexpectedly pop the blocking preview modal dialog, which is wrong for internal/batch housekeeping updates. Added an explanatory comment block in the tableextension file itself (not just this memory) documenting this reasoning so a future reader doesn't "helpfully" re-enable it without re-deriving all of this.
- **Gantt add-in path** (`codeunit_50615_GanttUpdateData.al`) — confirmed unchanged/already correct: reads `start_date`/`end_date` from DHTMLX JSON via direct field assignment (not `.Validate()`), computes Old/New dates itself, and explicitly calls `DayPlanningPeriodSyncMgt.HandleJobTaskPeriodChange(JobTask, OldStartDate, OldEndDate, false)` before persisting — this already routes through the (now-fixed) `CalculateChanges`, no changes needed to this file.
**How to apply:** If a third scenario ever needs "is this date schedulable" logic, call `Day Plannings Mgt.IsActiveWorkDay` rather than re-deriving weekday-active/calendar-exception logic inline — it's now the single shared entry point for Work-Hour-Template-based (as opposed to Day-Planning-Pattern-boolean-based) active-day checks. If the equal-vs-distinct naive-date cascade distinction in Pass 2 is ever touched again, re-read this entry fully before changing it — it's easy to accidentally regress either the shrink-clamp collapse behavior or the shift cascade behavior while "simplifying."

**Day 1..Day 7 / "Week Pattern" duplication fully removed (2026-07-16, later same day) — SUPERSEDES the "untouched"/"stay driving... Day1..7 booleans" claim in the entry directly above.** Both of the two "which weekdays are active" mechanisms that duplicated Work-Hour Template's Monday..Sunday hours fields are now gone:
- **Job Task's `"DayPlanning Week Pattern"` (field 50610 on tableextension 50605 "Job Task ext")** — deleted, along with its field control on `page_50618_JobTaskCard_Project.al` (group Planning/Duration). Confirmed dead beforehand (only the already-rewritten codeunit 50617 referenced it, and that rewrite — see entry above — had already switched to `JobTask."Work Hour Template"` instead).
- **`table_50607_DayPlanningPattern.al`'s `"Day 1".."Day 7"` booleans (fields 50-56) and their `local procedure BuildWeekPatern()`** — deleted.
- **`codeunit_50610_DayPlanningMgt.al`'s `ExpectedWeekDay`** — rewired to `exit(IsActiveWorkDay(DayPlanningGenerator."Work-Hour Template", NewTaskDate))`, i.e. it now delegates entirely to the same `IsActiveWorkDay` helper the period-change reschedule rework (entry above) introduced — no more separate `Day1-7`/`GetWeekDaysFromDayPlanningGenerator`-driven path. `GetWeekDaysFromDayPlanningGenerator` (the old boolean-to-temp-Integer-table builder) is deleted. The `OffOnWeekEndAndPublicHoliday: Boolean` parameter was also dropped from `ExpectedWeekDay` — its sole call site (`CreateDayPlanning`) always passed `true`, and `IsActiveWorkDay` has no "ignore calendar" mode to map a `false` onto, so the parameter would only ever have been a dead no-op. Behavior for the one real caller is unchanged.
- **`"Week Pattern"` field (57) on Day Planning Pattern — KEPT, as a fully derived/read-only (`Editable = false`) display, per explicit user confirmation.** This field went through 3 states in one session, worth recording so it isn't flip-flopped again: (1) originally a stored field manually rebuilt from the Day1-7 booleans via `BuildWeekPatern`; (2) briefly fully deleted alongside Day1-7 during a race condition between two concurrently-running agents (one agent's "keep it, derive it" edit lost to another agent's "delete everything" edit landing after it — not a deliberate design decision, just lost work); (3) **re-confirmed by the user as a requirement and re-implemented** — same field number (57), same `Code[13]` type, `Editable = false`. Rebuild logic is `codeunit_50610_DayPlanningMgt.al`'s `procedure GetActiveWeekdaysText(WorkHourTemplateCode: Code[20]): Code[13]` (public — reuses the existing `local procedure IsWeekdayActiveInTemplate` that `IsActiveWorkDay` already relies on, rather than re-deriving Work-Hour-Template weekday-hours access a third time), called from Day Planning Pattern's `"Work-Hour Template"` field `OnValidate` (`Rec."Week Pattern" := DayPlanningMgt.GetActiveWeekdaysText(Rec."Work-Hour Template")`) so it updates live the moment the template is picked/changed. Field control re-added to `src/page/Pag50639.DayPlanningPattern.al`'s repeater with an updated ToolTip (no longer references "Day 1 upto Day 7"). **This is the current, confirmed-final state — don't remove it again without a fresh explicit instruction.**
- **Test rework in `test/DayPlanningCreation.Test.Codeunit.al` (codeunit 60020):** shared `Initialize()`'s `TestWorkHourTemplateCode` ('DPCTWHT') now sets `Monday`..`Friday := 8`, `Saturday`/`Sunday := 0` (was previously only Default Start/End Time + Non Working Minutes, no weekday hours at all) — this is now the "normal Mon-Fri business hours" default most tests reuse unmodified. Two new dedicated helpers were added for the tests that need weekday-exclusivity isolation: `CreateMondayOnlyWorkHourTemplate()` (code `'DPCTWHTMO'`, only `Monday := 8`) used by `GivenQuantityOfLinesTwo_...` (previously isolated via `"Day 1" := true` alone), and `CreateTuesdayOnlyWorkHourTemplate()` (code `'DPCTWHTTU'`, only `Tuesday := 8`, Monday included in the zeroed-out rest) used by `GivenSingleDayNotCheckedAsWeekday_...` (previously "Day 1 (Monday) unchecked, Day 2 (Tuesday) checked but out of range" — the Tuesday-only template reproduces "Monday not a qualifying weekday" the same way). The other 4 tests just dropped their `"Day 1" := true`/`"Day N" := ...` lines outright (3 of them error out in `CreateDayPlanning` before `ExpectedWeekDay` is ever reached, so weekday-qualification was never actually load-bearing for those; the 4th, `GivenVendorNoSet...`, is a single-Monday scenario already covered by the shared Mon-Fri default). All 7 `[Test]` methods in codeunit 60020 confirmed passing via a REAL `al_run_tests` execution against NL_Test (not the usual skip/false-failure — see [[dailyoptimizer-test-framework-setup]], this run actually worked: tool reported "8 passed, 0 failed, 0 skipped" — 7 named test methods plus one unnamed aggregate/suite-level PASS line).
**How to apply:** Don't reintroduce a Day1-7-boolean-style STORED weekday pattern anywhere in this codebase — `Day Plannings Mgt.IsActiveWorkDay(WorkHourTemplateCode, Date)` is now the single, exclusive source of "is this weekday/date active" truth, for both the Day Planning Pattern expansion flow and the period-change reschedule flow. The one exception, confirmed by the user, is Day Planning Pattern's own `"Week Pattern"` field (57) — it IS a stored field, but purely as a cached/derived display (`Editable = false`, rebuilt via `GetActiveWeekdaysText` on every `"Work-Hour Template"` change, never independently editable) — this is intentional, not duplication, since it has exactly one writer and mirrors rather than competes with the Work-Hour Template as the source of truth.
**Concurrency note:** if multiple agents/sessions may ever work on this same branch simultaneously again, don't — the mid-task loss of this exact field was caused by two concurrent edits racing on the same files. Confirm no other agent (background or otherwise) is active on this repo before making further changes here.

**Job Task Gantt bar not reaching the full last day — client-side JS fix, NOT an AL data-model change (2026-07-16).** Symptom: a Job Task's rendered bar (e.g. Job 10000/Task 2020, Planned Start 2026-07-27, Planned End 2026-08-13) stopped short of covering its Planned End Date fully. Root-cause investigation confirmed via DHTMLX's own docs/forum that `task.end_date` is always EXCLUSIVE (the calendar day boundary AFTER the last day a task occupies) and that DHTMLX has **no built-in config flag** to make a bar render "full to the last day" — a DHTMLX staff forum reply explicitly recommends handling this in the `onTaskLoading` event, which is the approach taken here. A first attempt reshaped the AL↔JSON date contract itself (explicit `end_date = PlannedEndDate+1` outbound in `codeunit_50613_GanttChartDataHandler.al`, `-1 day` inbound in `codeunit_50615_GanttUpdateData.al`) — **this was reverted at the user's explicit request** in favor of a simpler, purely client-side fix; do not reintroduce that AL-side date-shifting without a fresh explicit ask. **Both AL files are back to their original, untouched form** (`duration`-only outbound JSON, no adjustment on the inbound `'end_date'` case) — see [[dailyoptimizer-test-framework-setup]]-adjacent files unaffected.
- **Actual fix, entirely in `src/dhx/ganttdemo2/wrapper.js`:** new top-level helper `_normalizeTaskBarDates(task)` (placed next to `_normalizeId`/`_darkenHex`) forces `task.start_date` to `00:00:00.000` and `task.end_date` to `23:59:59.999` **without shifting the calendar date portion of either** — it only touches time-of-day on whatever Date objects DHTMLX has already parsed/derived. Wired into three places: (1) `onTaskLoading` (~line 493) — covers every task on initial load/full reload, both leaf/Posting tasks and Begin-Total/parent tasks (both reach this same event; `Total`/`End-Total` task types are filtered out server-side in `GetJobTasksAsJson` and never reach the Gantt at all). (2) `onAfterTaskDrag` (~line 1015) and (3) `onAfterTaskUpdate` (~line 1051) — both re-apply the same normalization after a user drag/resize/lightbox edit, then call **`gantt.refreshTask(id)`** (NOT `gantt.updateTask(id)` — `updateTask` re-fires `onAfterTaskUpdate`/the DataProcessor and would recurse into another BC round-trip for what should be a pure visual correction; `refreshTask` repaints from the current in-memory task object without firing any event or DataProcessor call, confirmed via DHTMLX docs). This second hook exists because `gantt.config.round_dnd_dates` (default on) re-snaps a dragged edge's time-of-day back to midnight on every drag, which would otherwise silently undo the normalization for whichever edge the user just moved until the next full reload.
- **Outbound payload building (`fmt(task.end_date)` in the drag/update handlers, using `gantt.date.date_to_str("%Y-%m-%d")`) is unaffected by the time-of-day change** — that formatter only reads the year/month/day components, so BC never sees or is affected by the 23:59:59 stamp; this is purely a rendering-layer fix, confirmed safe against the AL round-trip without needing to touch AL at all.
- **Checked and confirmed NOT affected / out of scope:** `_renderRequestBars()` (the black overlay bars for Day-Planning "Request"/placeholder-date entries within a Job Task's row) is a separate single-day overlay mechanism — it only reads `task.start_date`/`task.end_date` for the row's `top`/`height` (vertical position), and computes its own day-box `left`/`width` fresh from `dt.placeholder_date` + `gantt.date.add(pd, 1, "day")` (already correctly exclusive on its own terms) — untouched, no fix needed there. The Day Planning resource-panel timeline (hour-level single-day scheduling, a different concept entirely) is also unrelated.
- **Open/unverified edge case, flagged honestly rather than silently assumed fine:** whether DHTMLX's parent/project-task dual-bar rendering (`gantt_task_line_planned`/`gantt_task_line_actual`, from the `auto_scheduling` plugin, see the color-handling comment right above `onTaskLoading`) re-derives its own dates from a children-rollup AFTER `onTaskLoading` fires (in which case it would likely inherit the fix for free, since it would just be reusing already-normalized child dates) versus reading `task.start_date`/`task.end_date` directly (in which case it's already covered) could not be confirmed without a live browser session — the specific reported example (Task 2020, a plain numbered leaf task per this codebase's Posting-task-numbering convention) is very likely NOT a parent/Begin-Total task, so the primary fix directly addresses the reported bug regardless.
- **Also flagged, not silently swept under the rug:** the user separately reported the short-bar symptom is **not uniform** across all bars (some already render full, some don't) before this fix — a real per-record discrepancy that a pure time-of-day normalization cannot explain or fully rule out (e.g. it would not fix a task whose stored `Job Task.Duration` is itself stale/wrong by a full day, if such a record exists — a data bug, not a rendering bug). This JS fix guarantees every bar renders as a full 00:00:00–23:59:59 span of whatever start/end DATES DHTMLX currently computes; if some bar is still short afterward, that points to a stale-`Duration`/date data issue on that specific Job Task record, not a rendering issue, and would need a separate AL-side data audit (not attempted here — out of scope for this fix, no live-browser or DB-query access was available to check specific record values in this session).
**How to apply:** Don't reintroduce AL-side `end_date`±1-day shifting in `codeunit_50613`/`codeunit_50615` for this — it was tried and explicitly reverted in favor of this wrapper.js-only approach. If Day Planning's own JSON (`CreateDayPlanningJsonObject`) or any other Job-Task-bar-adjacent JS rendering path is added later, reuse `_normalizeTaskBarDates` rather than hand-rolling date math again.

**Gantt "losing the user's place on data refresh" — raw scroll-coordinate approach tried TWICE, abandoned; replaced with task-identity re-focus (2026-07-16, `src/dhx/ganttdemo2/wrapper.js`).** Two structurally-sound attempts at capturing/restoring raw scroll coordinates across a full reload cycle (`ClearData()` → `LoadAllData()` → ... → `RenderGantt()`) were both confirmed NOT to work by live user testing, despite correct call-order reasoning both times: (1) captured inside `LoadProjectData` and restored via `setTimeout(fn, 0)` — no effect; (2) moved capture to `ClearData()` (the true first step) and restore to `RenderGantt()` (the true last step, after its own `gantt.render()`), with a bounded 5-retry/100ms loop for the resource-panel scrollTop piece — still no effect. **Don't attempt a third variation of raw scroll/pixel coordinate restoration for this problem** — something about this DHTMLX build's scroll/coordinate APIs doesn't behave as documented, or a factor neither attempt accounted for is in play.
- **Replacement approach (current, implemented):** re-anchor on task IDENTITY instead of coordinates. `onTaskClick` (~line 896) now also records the clicked id into module-level `var _lastClickedTaskId` (only when the click resolved to a real bar click, `onBar === true`, not a grid-row click). `RenderGantt()` calls new `_restoreTaskFocusAfterReload()` (after its own `gantt.render()`) which, if `_lastClickedTaskId` is set and `gantt.isTaskExists(id)` is true, calls `gantt.showTask(id)` (scrolls it into view) and `gantt.selectTask(id)` (visually selects it) — both guarded with `if (gantt.showTask)`/`if (gantt.selectTask)` existence checks since this exact DHTMLX build's API surface hasn't been verified live (no browser available in this environment). If the task no longer exists (e.g. deleted), skips silently. All raw-coordinate code (`_savedScroll`/`_savedResScrollTop` vars, the capture block in `ClearData()`, `_restoreScrollAfterReload()` and its call in `RenderGantt()`) was fully removed, not commented out.
- **`onGanttReady` considered and rejected as the refocus trigger:** it's registered once inside `BOOT()` (file's own header comment: "BOOT() - runs once") and DHTMLX's `onGanttReady` itself fires once, when `gantt.init()` finishes — not on every reload. `RenderGantt()` (which AL calls explicitly as the literal last step of every refresh cycle) is the reliable per-reload signal instead.
**How to apply:** If a user reports this still isn't working after this change, the bug is likely in `gantt.showTask`/`gantt.selectTask` behavior in this specific DHTMLX build (not in the identity-tracking or call-order logic, which mirror already-proven patterns elsewhere in this file) — don't reflexively go back to scroll coordinates a third time without new information from the user about what specifically fails.

**Job Task `Progress` (%) now populated by demo data + backfillable via report 50600 (2026-07-16).** Field 50601 "Progress" on tableextension 50605 "Job Task ext" (`Integer`, MinValue=0/MaxValue=100, default 0/"not filled") was previously never set anywhere. Scope, confirmed by user: applies ONLY to `"Job Task Type"::Posting` rows, never Total/Begin-Total/End-Total header rows.
- `codeunit_50602_CreateDemoData.al`'s central `AddTask(var JT; TaskNo; Desc; StartDate; EndDate; TaskType)` helper (~line 751, every demo Job Task in the app funnels through this one procedure) now sets `JT.Progress := Random(101) - 1` (Random(101) yields 1..101, so -1 gives the needed 0..100 inclusive range) right before the `Insert()`/`Modify()` call, guarded by `(TaskType = TaskType::Posting) and (JT.Progress = 0)` — the `= 0` check is a correctness/safety guard, not expected to skip rows in practice since every Posting task starts unfilled on a full demo-data regen.
- `report_50600_RepairDayPlanningResourceGroup.al` ("RepairData") gained `local procedure BackfillJobTaskProgress(): Integer` (same random-range technique, `SetRange("Job Task Type", Posting)` + `SetRange(Progress, 0)`, loop/Modify/count — mirrors `RefreshJobPlanningLineUnitPrice`'s structural shape) and is now the report's ACTIVE call in `OnPreReport()`, per this report's established "one active backfill call at a time, others commented out not deleted" convention (see the original 2026-07-09 entry above) — `RefreshJobPlanningLineUnitPrice()`'s call+Message is now the one commented out (joining the already-commented `BackfillInvoiceJobLedgerEntryNo`). Permissions gained `tabledata "Job Task" = rm`.
**How to apply:** If a NEW backfill/repair procedure is added to this report later, follow the same rotation — comment out `BackfillJobTaskProgress`'s call (don't delete the procedure itself), make the new one active. If `AddTask` ever needs a similar "only for Posting rows" per-field default again, reuse this same `TaskType = TaskType::Posting` guard pattern rather than re-deriving it.

**Resource Absence Management (added 2026-07-17), design reviewed/approved via `docs/generate_resource_absence_management_doc.py`.** Table 160 "Res. Capacity Entry" now doubles as the Absence ledger: Absence rows are ordinary rows with `Type = Absence` (new field 50605, `Enum 50682 "Res. Capacity Entry Type"` — Capacity=0 default/Absence=1) and a NEGATIVE value in the existing `Capacity` field, so every pre-existing SUM/FlowField/query reader nets Capacity-minus-Absence automatically with zero changes elsewhere. Field 50606 "Absence Reason Code" (`Code[10]`, `TableRelation = "Cause of Absence"`, base table 5206 — the same reason table standard Employee Absence uses) is mandatory on Absence rows only, enforced in code (not a table-level `NotBlank`) since it must stay optional on Capacity rows. Deliberately NO `Capacity Entry No.` link field and NO stored `Resource Name` FlowField — both explicitly rejected as redundant during design review; don't add them back if asked to "complete" this feature later.
- **New objects:** Enum 50682 "Res. Capacity Entry Type" (`src/enum/enum_50682_ResCapacityEntryType.al`). Codeunit 50686 "Resource Absence Mgt." (`src/codeunit/codeunit_50686_ResourceAbsenceMgt.al`) — single public `ValidateAndPrepareAbsence(var ResCapacityEntry; Hours: Decimal)` used for BOTH insert and modify (Card page calls it from both `OnInsertRecord`/`OnModifyRecord`): validates Hours>0 and Absence Reason Code non-blank, finds the matching Capacity row for that Resource No./Date (`Type=Capacity, Capacity>0`, `FindFirst` — takes whichever Capacity row sorts first by Entry No. if a resource has multiple split-shift Duplicate Ids for the same day; there's no UI to pick a specific one), copies its `"Duplicate Id"` onto the Absence row, sums ALL existing Absence rows for that Resource/Date/Duplicate-Id (excluding the row's own Entry No. via `SetFilter("Entry No.", '<>%1', ...)` when Entry No.<>0, i.e. on Modify) for the cumulative overshoot guard, then writes `Capacity := -Hours`. Same codeunit also owns an `[EventSubscriber(Table::"Res. Capacity Entry", 'OnBeforeDeleteEvent')]` that blocks deleting a Capacity row (Type=Capacity only, skips Absence-row deletes) while any Absence row still exists for the same Resource No./Date/Duplicate Id.
- Page 50684 "Resource Absence List" (`src/page/page_50684_ResourceAbsenceList.al`, `SourceTableView = where(Type = const(Absence))`, `CardPageId` → 50685) and Page 50685 "Resource Absence Card" (`src/page/page_50685_ResourceAbsenceCard.al`) — Card's "Hours" field is a page-level `Decimal` var (`HoursVar`, NOT bound to `Rec.Capacity` since users enter positive hours but the field is stored negative), populated as `-Rec.Capacity` in `OnAfterGetRecord` for existing rows and left blank (no auto-fill of remaining capacity — explicitly confirmed with user) for new ones via `OnNewRecord`; "Resource Name" is likewise a page-level `Text[100]` var populated by a local `Resource.Get()` lookup, not stored. `"Resource No."` is `Editable = Rec."Entry No." = 0` (locked after first save). `Type` is never user-editable — hard-set to `Absence` in `OnNewRecord`.
- Pageextension 50605 "ResourceCard Opti" (`src/pageext/pageext_50605_ResourceCard.al`) gained action `"Absence"` (`addafter("Day Plannings (Visual)")`, `RunObject = page "Resource Absence List"`, `RunPageLink = "Resource No." = field("No."), Type = const(Absence)`) plus a matching promoted `actionref`.
- Page 50629 "Res. Capacity FactBox Part" gained a `Type` repeater column (between Date and Capacity) so Capacity/Absence rows are visually distinguishable.
- **Real bug found+fixed during this feature, not just additive:** page 50627 "Resource Capacity Settings Opt" ("Update Capacity" wizard action) does a `CalcSums(Capacity)` to compute "existing capacity" before inserting an adjustment-delta row, in BOTH the `NoOfDuplicates>1` loop branch and the single-duplicate `else` branch — without a `Type=Capacity` filter, an existing Absence row's negative value would get silently netted into that sum, causing the wizard to insert a delta that cancels the recorded absence next time it's re-run for that resource/date. Fixed by adding `ResCapacityEntry.SetRange(Type, ResCapacityEntry.Type::Capacity);` right before each of the two `CalcSums(Capacity)` calls — purely additive, no other logic in that action touched.
- **Verification note:** `al_compile` threw a phantom `AL0185 Page 'Resource Absence Card' is missing` for page 50684 (referencing 50685's `CardPageId`) even after both files existed and were correctly named/ID'd — this is the known [[al_compile_stale_cache_quirk]]; `al_build` succeeded cleanly (produced the `.app`) in the same session with the same files, confirming the compile error was stale-cache noise, not a real problem. Don't chase this specific error pattern (CardPageId/RunObject "page X is missing" for a page that verifiably exists) without re-running `al_build` first.
**How to apply:** If Absence needs a "pick which Duplicate Id" UI later (currently always auto-picks the first-found Capacity row), that's a Card-page + codeunit signature change, not a data-model change — the Duplicate Id field itself already exists on table 160 from the pre-existing Capacity feature.
